# OneSheet 핸드오프

다음 세션 에이전트/인간을 위한 인수인계.  
작성 시점: 2026-08-05 · 브랜치 `cursor/launchpad-grid-4663`  
base `main`

---

## 1. 제품이 무엇인가

**OneSheet** = Teenage Engineering에 영감받은 **기타 악보 편집/재생기**.

- 슬로건/정체성: 장난감처럼 만지고, 바로 들어보는 **한 장 차트**
- 소리·문법은 **Strudel** (`@strudel/web`)이 담당
- OneSheet의 질문은 “Strudel을 연동하느냐”가 아니라 **그 문법을 얼마나 직관적으로 만지게 하느냐**

필수 규칙: `.cursor/rules/onesheet-paradigm.mdc` (`alwaysApply: true`)

---

## 2. 절대 잊지 말 것

1. **코드는 현상**이다. 정체성에 안 맞으면 백지화한다.
2. **간단 > 복잡**. 더하기 전에 빼라.
3. **TE 복제 ≠ TE 접근**. PO PCB/실크를 베끼지 마라. 빌릴 것은 **렌즈·한 겹 4×4·즉시 반응·작은 디스플레이**다.
4. **해체 루프**로 설계한다.
5. **재료(차트)는 문맥에 가두지 않는다.** LCD 비드 줄 + 차트 모드로 항상 닿을 수 있게.
6. 응답/UI는 **한국어** 우선.

---

## 3. 현재 UI / 모델 (런치패드)

### 화면 구조 (PO식 한 겹)
- **상단**: 브랜드 · 선택 위치
- **LCD**: 선택 칸 코드 · 모드 · **16칸 비드(차트 요약, 항상 선택 가능)** · 조성/음색/BPM
- **본체 4×4**: 모드에 따라 **패드 의미가 바뀜** (별도 팔레트 행 없음)
  - **차트**: 16 타임슬롯 (마디×박)
  - **도수**: I–vii° + rest (나머지 칸 ghost)
  - **진행**: Pop/50s/… 프리셋 8개
- **하단 transport**: `재생` · `메트로` · `차트` · `도수` · `진행`

### 데이터 (`src/sheet.ts`)
```ts
SheetState = {
  bpm, key,           // 메이저 조만 (C G D A E F Bb)
  degrees: (number|null)[16],  // 0–6 diatonic or rest
  voice, gain,        // 웜/샤프/소프트/피아노 → sound+cutoff
  metro: boolean
}
```

### 엔진 (`src/engine.ts`)
- 재생 중 편집 → `evaluate` 재평가

---

## 4. 버려진 길 (다시 가지 말 것)

| 시도 | 왜 버려졌나 |
|------|------------|
| PO PCB/실크 코스프레 | 외형 복제. 접근이 아님 |
| 차트 그리드 + 아래 별도 팔레트 행 | 두 겹. 세로로 길고 런치패드가 아님 |
| Part/Form 대시보드식 다크 UI | 장난감·한 장 감각 없음 |
| 코드명 사이클 / 근음×퀄리티 전부 노출 | 기준 없거나 제품에 과함 |
| bd→sd→hh 리듬 / 그루브 그리드 | 기타 차트 정체성과 안 맞음 |

---

## 5. 해체로 얻은 공식

**조작**
- ❌ 차트 그리드 + 팔레트 행  
- ✅ **하나의 4×4** + transport 렌즈가 의미를 바꿈  
- ✅ 차트 재료는 LCD 비드 / 차트 모드로 항상 접근

**코드**
- ✅ `조성 → 도수 토글 → 진행 프리셋`

**시간**
- ✅ `타임그리드(4×4 4분) + 메트로놈`

---

## 6. 다음 세션 후보

1. **주법 해체** — 음색 → 스트럼/아르페지오 (렌즈로 4×4에 올릴지)
2. **재생 피드백** — 플레이헤드를 비드/패드에
3. **조성 UX** — 카포/오도원
4. **사운드 품질** 검증
5. **Firebase Hosting** 정리
6. **모바일** 터치 타겟·간격

---

## 7. 로컬 명령

```bash
npm ci
npm run dev      # :5173
npm run build
```

---

## 8. 핵심 파일

| 경로 | 역할 |
|------|------|
| `.cursor/rules/onesheet-paradigm.mdc` | 사고 패러다임 |
| `src/sheet.ts` | 차트 모델 · `toStrudel` |
| `src/App.tsx` | 런치패드 UI · 모드 재매핑 |
| `src/App.css` | 서피스 |
| `src/engine.ts` | Strudel evaluate/hush |
| `README.md` | 정체성만 |

---

## 9. 한 줄 인수인계

> OneSheet는 **하나의 4×4 런치패드**다. 모드는 팔레트 행이 아니라 **같은 패드의 의미**를 바꾼다. 차트는 LCD 비드와 차트 렌즈로 가두지 않는다.
